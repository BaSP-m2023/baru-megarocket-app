import styles from './activities.module.css';

function Activities() {
  return (
    <section className={styles.container}>
      <h2>Activities</h2>
    </section>
  );
}

export default Activities;
